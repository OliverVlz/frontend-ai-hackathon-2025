{
  "name": "angular-app-heroku",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "npm install -g @angular/cli && npm install && ng build --configuration=production"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "22.14"
  }
}
