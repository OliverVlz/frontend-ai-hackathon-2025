{
  "name": "angular-app-heroku",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd project && npm install && npm install -g @angular/cli && ng build --configuration=production"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "22.14"
  }
}
